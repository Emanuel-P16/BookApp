<h2>Recently Added</h2>
<div>
    <% books.forEach(book => { %>
      <a href="/books/<%= book.id %>">
       <img  alt="" height="150" width="100" src="<%= book.coverImagePath %>">
      </a>
    <%   })%>
    </div>